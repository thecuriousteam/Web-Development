let a = -1;

if (a > 0) {
	console.log(`The number ${a} is a positive number`);
} else if (a === 0) {
	console.log(`The number ${a} is equal to zero`);
} else {
	console.log(`The number ${a} is a negative number`);
}
