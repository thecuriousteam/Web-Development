let num = "two";

switch (num) {
	case "one":
		console.log(`The number ${num} is 1`);
		break;

	case "two":
		console.log(`The number ${num} is 2`);
		break;

	default:
		console.log(`The number ${num} is not valid`);
}
